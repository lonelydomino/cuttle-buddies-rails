<%= stylesheet_link_tag "users" %>

<div class="header">
<% 10.times do |f| %>
  <%= image_tag get_random_fish_image, class: "tank-image" %>
<% end %>
  
</div>

<div class="topnav">
  <%= link_to "All Users", users_path %>
  <%= link_to "Replace this" %>
  <%= link_to "Replace this too" %>
</div>

<div class="row">
  <div class="column">
  <div class="user-box">
    <h2>Partners</h2>
    <p> 
        <%# <% if !@user.partners.empty? %> %>
            <%# <% @user.partners do |p|  %> %>
                    <%# <%= p.name %> %>
            <%# <% end %>  %>
        <%# <% else %> %>
            Add partner(s)!
        <%# <% end %>  %>
    </p>
  </div>
    </div>
  <div class="column">
   <div class="user-box">
    <h2>News</h2>
    <p>
        Welcome to CuttleBuddies Beta!
    </p>
  </div>
    </div>
  <div class="column">
   <div class="user-box">
    <h2>Friends</h2>
    <p>
      
        <% if @user.friends.empty? %>
          <%= form_with(url: users_path, method: :get) do |f|%>
            <%= f.hidden_field :search, value: "" %>
            <button class="friends-search-button" type="submit">Search for friends!</button>
          <% end %>
        <% else %> 
          <% @user.friends.each do |f| %>
          <%= f.username %>
          <% end %>
        <% end %>
    </p>
  </div>
    </div>
</div>