
<%# <% @fish.each do |f| %>
    <%# <%= f.name %><br> %>
    <%# <%= image_tag ("/assets/tank/#{f.image_path}") %> %>
<%# <% end %> %> %>

<%= image_tag ("/assets/tank/#{Fish.order(Arel.sql('RANDOM()')).first.image_path}") %>
<%= form_with(url: shop_index_path, method: :post) do |f| %>
    <%= f.submit 'Save', data: { confirm: 'Roll lottery?' } %>
<% end %>